 <%= simple_form_for([@post, comment.new], html: { class: 'form-horizontal' })  do |f| %>                
          
          <%= f.input :body, placeholder: "Comment on this post" %>

          <div class="form-group">
            <%= f.button :submit %> 
          </div>

    <% end %>

    <!-- topic, post, post.comments.new -->