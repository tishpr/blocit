 
 <%= simple_form_for([post, post.comments.new], remote: true, html: {class: "form-horizontal"}) do |f| %>     
          <%= f.input :body, placeholder: "Comment on this post" %>

          <div class="form-group">
            <%= f.button :submit, {class: 'btn btn-success'} %> 
          </div>

    <% end %>

 